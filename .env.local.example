# Supabase (optional — app falls back to built-in defaults without it)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Absolute URL of the deployed app (for Teams banner images)
NEXT_PUBLIC_APP_URL=

# NWR Realtime Performance Data (Rail Data Marketplace) — live perf feed.
# This is the "Consumer key" from your RDM subscription's API access
# credentials page. Server-side only: NO NEXT_PUBLIC_ prefix, never commit it.
RDM_API_KEY="J5qzciARrZAEAqmj76jdypx6VnwYUAYPimUAFxn00LieV6lM"

# Optional: override the data product base URL (defaults to the v4.0 product
# URL in src/lib/rdm/config.ts)
# RDM_API_BASE=
